{
	"version": "0.2.0",
    "configurations": [
        {
            "name": "Browserlist config highlighter",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ]
        }
    ]
}